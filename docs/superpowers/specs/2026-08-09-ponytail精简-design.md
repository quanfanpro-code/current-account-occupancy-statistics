# Ponytail 精简设计

## 方案比较

1. 推荐：各仓就地删除未调用能力，保留正式接口和深海蓝输出。
2. 引入共享 Excel 包。会增加跨仓耦合和发布成本，本次不采用。
3. 仅删除文档。无法完成未使用依赖和生产辅助方法清理，本次不采用。

## 设计

保留 make_excel 的正式数据写入、格式、公式和进度能力，删除 beautify、独立 CLI 和多主题选择。删除 xlsxwriter 安装项、未使用 NumPy 导入及只为测试存在的 build_from_balance；测试改走正式 build_bundle。

双 GUI 回退、线程池、多核配置和六 Sheet 输出不变。所有删除均可从父提交恢复。
